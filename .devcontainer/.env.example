# Tailscale Auth Key
# Get this from your Tailscale admin console: Settings > Keys
# Make sure to enable "Reusable" when generating the key
TS_AUTHKEY=tskey-auth-your-key-here

# Timezone (optional, defaults to America/Los_Angeles)
TZ=America/Los_Angeles

# GitHub Remote URL (automatically extracted from git remote origin)
# Run: git remote get-url origin
GITHUB_REMOTE_URL=https://github.com/your-username/your-repo